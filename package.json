{
  "name": "structure-viewer",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for Minecraft structure viewer libraries built on deepslate: performance patches, fade rendering, structure splitting, and self-hosted asset pipeline.",
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "tsup": "^8.3.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  },
  "license": "Apache-2.0"
}
